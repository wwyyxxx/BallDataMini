<view >
    <video style="width:100%" id="myVideo" src="{{video.url}}" controls></video>
    <van-cell title="{{video.title}}" value="{{video.type}}" size="large" label="{{video.summary}}" />
</view>
